Extract 5 to 15 keywords or key phrases from the following text.

Rules:
- Focus on the most important topics, concepts, technologies, and themes
- Include both single words and short phrases (2-3 words max)
- Prefer specific terms over generic ones (e.g. "transformer architecture" over "technology")
- Only include keywords that are clearly relevant to the main content
- Do NOT include proper nouns that are better classified as named entities (people, companies)
- If the text is too short or vague, return fewer keywords
- If no meaningful keywords can be extracted, return an empty array

Return ONLY a JSON array of strings with no other text:
["keyword1", "keyword2", ...]

Text:
{content}
